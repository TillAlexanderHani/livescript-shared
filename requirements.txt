# Python deps for local runs. On GitHub Actions, torch is installed from the
# CPU-only index first (see workflow) to avoid pulling CUDA wheels.
openai-whisper
torch
feedparser
requests
python-dateutil
